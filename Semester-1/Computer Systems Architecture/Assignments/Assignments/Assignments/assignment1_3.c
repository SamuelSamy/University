// 3.Write a program that computes out the largest and smallest values of 5 numbers read from the input.

#pragma warning(disable:4996)

#include <stdio.h>
#include <limits.h>
/*
int main()
{
    int maxim = INT_MIN, minim = INT_MAX, aux;

    for (int i = 0; i < 5; i++)
    {
        scanf("%d", &aux);

        if (aux > maxim)
        {
            maxim = aux;
        }

        if (aux < minim)
        {
            minim = aux;
        }
    }

    printf("Maxim: %d, Minim: %d", maxim, minim);
    return 0;
}*/