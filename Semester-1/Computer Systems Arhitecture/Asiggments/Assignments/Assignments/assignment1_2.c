// 2.Read two integers and compute their sum, average and sum of the squares of the numbers.

#pragma warning(disable:4996)

#include <stdio.h>
/*
int main()
{
    int a, b;

    scanf("%d %d", &a, &b);

    int sum = a + b;
    double average = 1.0 * sum / 2;
    int sum_sqaures = a * a + b * b;

    printf("Sum: %d, Average: %.3f, Sum of the squares: %d", sum, average, sum_sqaures);
    return 0;
}*/